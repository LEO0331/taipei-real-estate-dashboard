import{mkdir,writeFile}from'node:fs/promises';const d='data/raw/declared-land-value-records';await mkdir(d,{recursive:true});const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=e96325f8-7c25-4369-9ee2-04a0ad9fb551',{signal:AbortSignal.timeout(30000)});if(!r.ok)throw new Error(`HTTP ${r.status}`);await writeFile(`${d}/source.csv`,Buffer.from(await r.arrayBuffer()));
