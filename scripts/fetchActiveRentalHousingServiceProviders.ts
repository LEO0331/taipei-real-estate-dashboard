import{mkdir,writeFile}from'node:fs/promises';const d='data/raw/active-rental-housing-service-providers';await mkdir(d,{recursive:true});const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=a80d1e05-f188-4684-acc6-6695603a95fa',{signal:AbortSignal.timeout(30000)});if(!r.ok)throw new Error(`HTTP ${r.status}`);await writeFile(`${d}/source.csv`,Buffer.from(await r.arrayBuffer()));
