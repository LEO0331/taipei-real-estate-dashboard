import{mkdir,writeFile}from'node:fs/promises';const d='data/raw/general-expropriation-compensation-custody';await mkdir(d,{recursive:true});const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=15c6d879-50e9-4646-91b4-ca90aa16848c',{signal:AbortSignal.timeout(30000)});if(!r.ok)throw new Error(`HTTP ${r.status}`);await writeFile(`${d}/source.csv`,Buffer.from(await r.arrayBuffer()));
