import { mkdir, writeFile } from 'node:fs/promises'; import { listCsvFiles } from './data.ts'; const d='data/raw/real-estate-appraiser-directory';await mkdir(d,{recursive:true});if(!(await listCsvFiles(d)).length||process.argv.includes('--force')){const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=de252802-daa0-4074-898b-96db79485e70',{signal:AbortSignal.timeout(30_000)});if(!r.ok)throw new Error(`HTTP ${r.status}`);await writeFile(`${d}/source.csv`,Buffer.from(await r.arrayBuffer()))}
