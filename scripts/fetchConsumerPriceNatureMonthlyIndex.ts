import{mkdir,writeFile}from'node:fs/promises';const d='data/raw/consumer-price-nature-monthly-index';await mkdir(d,{recursive:true});const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=a4e912e9-ad31-4e01-babd-6358dcc9b65c',{signal:AbortSignal.timeout(30000)});if(!r.ok)throw new Error(`HTTP ${r.status}`);await writeFile(`${d}/source.csv`,Buffer.from(await r.arrayBuffer()));
